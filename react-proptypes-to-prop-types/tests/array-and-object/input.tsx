import React from "react";

Component.propTypes = {
  items: React.PropTypes.array,
  config: React.PropTypes.object,
  id: React.PropTypes.oneOf([1, 2]).isRequired,
};

function Component() {
  return null;
}
