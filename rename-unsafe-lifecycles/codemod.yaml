schema_version: "1.0"

name: "@react-codemods/rename-unsafe-lifecycles"
version: "0.1.0"
description: "Rename deprecated lifecycle methods to UNSAFE_ prefixed versions"
author: "React Codemods"
license: "MIT"
workflow: "workflow.yaml"


targets:
  languages: ["tsx"]

keywords: ["transformation", "migration"]

registry:
  access: "public"
  visibility: "public"

capabilities: []
