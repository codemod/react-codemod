schema_version: "1.0"

name: "@react-codemods/replace-use-form-state"
version: "0.1.0"
description: "Rename useFormState to useActionState in react-dom imports and usages"
author: "React Codemods"
license: "MIT"
workflow: "workflow.yaml"

targets:
  languages: ["tsx"]

keywords: ["transformation", "migration", "react", "useFormState", "useActionState"]

registry:
  access: "public"
  visibility: "public"

capabilities: []
