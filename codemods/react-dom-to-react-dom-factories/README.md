# @react/react-dom-to-react-dom-factories

Replace `React.DOM.*(...)` and `DOM.*(...)` factory calls with `createElement(...)`.

## Usage

```bash
codemod run @react/react-dom-to-react-dom-factories
```

## Development

```bash
pnpm test
pnpm check-types
```
