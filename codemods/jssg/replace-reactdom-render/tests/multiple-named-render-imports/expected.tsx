import { createRoot } from "react-dom/client";
import { render } from "react-dom";
import { render as render2 } from "react-dom";

const root = createRoot(node1);
root.render(<A />);
const root1 = createRoot(node2);
root1.render(<B />);
