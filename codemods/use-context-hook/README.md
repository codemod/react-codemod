# @react/use-context-hook

Transform `React.useContext()` and `useContext()` calls into `use()`.

## Usage

```bash
codemod run @react/use-context-hook
```

## Development

```bash
pnpm test
pnpm check-types
```
