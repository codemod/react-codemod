# @react/rename-unsafe-lifecycles

Rename deprecated React lifecycle names to their `UNSAFE_` equivalents in class components, object literals, and member references.

## Usage

```bash
codemod run @react/rename-unsafe-lifecycles
```

## Development

```bash
pnpm test
pnpm check-types
```
