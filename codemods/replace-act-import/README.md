# @react/replace-act-import

Move `act` usage from `react-dom/test-utils` to `react` across named imports, namespace imports, and re-exports.

## Usage

```bash
codemod run @react/replace-act-import
```

## Development

```bash
pnpm test
pnpm check-types
```
