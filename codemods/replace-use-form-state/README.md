# @react/replace-use-form-state

Rename `useFormState` to `useActionState` in `react-dom` imports and usages.

## Usage

```bash
codemod run @react/replace-use-form-state
```

## Development

```bash
pnpm test
pnpm check-types
```
