# @react/replace-reactdom-render

Replace `ReactDOM.render` and named `render` imports with `createRoot(...).render(...)`.

## Usage

```bash
codemod run @react/replace-reactdom-render
```

## Development

```bash
pnpm test
pnpm check-types
```
