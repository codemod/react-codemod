schema_version: "1.0"

name: "@react/remove-forward-ref"
version: "0.1.0"
description: "Inline forwardRef render functions and move ref into props"
author: "Codemod <hello@codemod.com>"
license: "MIT"
workflow: "workflow.yaml"

targets:
  languages: ["tsx"]

keywords: ["React", "forwardRef"]

registry:
  access: "public"
  visibility: "public"

capabilities: []
