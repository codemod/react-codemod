# @react/remove-forward-ref

Inline `forwardRef(...)` render functions and pass `ref` through props.

## Usage

```bash
codemod run @react/remove-forward-ref
```

## Development

```bash
pnpm test
pnpm check-types
```
