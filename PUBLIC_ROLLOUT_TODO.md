# Public Rollout TODO

- Port historical execution counts in DB. Owner: Mo
- Update publisher display name to `Codemod` in DB. Owner: Mo
- Confirm all `react-*` and `react-19-*` registry pages are public and searchable.
- Confirm recipe uses the final public package names.
- Update public docs/README links if registry URLs changed after renaming.
- Do one clean smoke run of `react-19-migration-recipe` from the registry.
- Inform React folks and share the final package list.
- Monitor first public executions for install, search, and workflow issues.
