# Public Rollout TODO

- Port historical execution counts in DB. Owner: Mo
- Update publisher display name to `Codemod` in DB. Owner: Mo
- Create codemod aliases from legacy to new codemods. Owner: Mo
- ~Confirm all `react-*` and `react-19-*` registry pages are public and searchable. Owner: Mohab~
- ~Confirm recipe uses the final public package names. Owner: Mohab~
- ~Update public docs/README links if registry URLs changed after renaming. Owner: Mohab~
- ~Do one clean smoke run of `react-19-migration-recipe` from the registry. Owner: Mohab~
- Inform React folks and share the final package list. Owner: Alex
- Update legacy `react/react-codemod` repo README to point users to the new repo. Owner: Mohab
- Monitor first public executions for install, search, and workflow issues. Owner: TBD
